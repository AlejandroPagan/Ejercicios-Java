//*
Necesitamos sumar números grandes y requerimos tu ayuda.

Escribe una función que devuelva la suma de dos números.
Los números de entrada son cadenas (strings) y la función debe devolver también una cadena.

Ejemplos:
add("123", "321"); -> "444"
add("11", "99");   -> "110"

Notas:
- Los números de entrada pueden ser muy grandes.
- La entrada será siempre una cadena de dígitos.
- Los números son positivos.
- No se permite el uso de BigInteger.
*/
public class Kata {
    public static String add(String a, String b) {
        // Tu solución aquí
    }
}
public class Ejercicio5 {
  public static String add(String a, String b) {

    StringBuilder resultado = new StringBuilder();

    int num1 = a.length() - 1;
    int num2 = b.length() - 1;
    int resto = 0;
    int val1, val2, suma;

    while (num1 >= 0 || num2 >= 0 || resto > 0) {
      val1 = (num1 >= 0) ? a.charAt(num1--) - '0' : 0;
      val2 = (num2 >= 0) ? b.charAt(num2--) - '0' : 0;

      suma = val1 + val2 + resto;

      if (suma >= 10) {
        resto = 1;
        resultado.append(suma - 10);
      } else {
        resultado.append(suma);
        resto = 0;
      }
    }

    // Invertimos el StringBuilder y eliminamos ceros iniciales
    String finalResult = resultado.reverse().toString();
    while (finalResult.length() > 1 && finalResult.charAt(0) == '0') {
      finalResult = finalResult.substring(1);
    }

    return finalResult;
  }
}
