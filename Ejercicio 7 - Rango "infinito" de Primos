//Crea un flujo (stream) infinito de números primos, un poco como `IntStream.of(2,3,5,7,11,13,17)`, pero infinito. El flujo debe ser capaz de generar un millón de primos en unos pocos segundos.

import java.util.stream.IntStream;

public class Primes(){
public static IntStream(){
  return IntStream.rangeClosed(2,Integer.MAX_VALUE).filter(Primes:isPrime);
}
public static boolean isPrime(int number){
  for (int i = 2; i<Math.sqrt(number) +1; i++){
    if(number%i == 0) return false;
  }
  return true;
}
}
